const Footer = ({className}) => {
  return (
    <footer className={` bg-white border text-black p-1 flex justify-center text-center w-fit mx-auto px-10 rounded-t-full border-primary drop-shadow-lg relative z-10 text-xs md:text-base ${className}`}>
      2023 © REFLEXSCALE. All rights reserved by REFLEXSCALE
    </footer>
  );
};

export default Footer;
